


class Track:
    def __init__(self, name, path):
        self.name = name
        self.path = path

    def playsound(self):
        return 'memes'

        # sound = pygame.mixer.Sound(self.song)
        # clock = pygame.time.Clock()
        # sound.play()
        # while True:
        #     clock.tick(1)